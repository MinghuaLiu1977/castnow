name: castnow_core
description: "CastNow Shared Core — screens, widgets, services, and localization"
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: ^0.12.5
  peerdart: ^0.5.6
  permission_handler: ^11.3.1
  wakelock_plus: ^1.1.2
  uuid: ^4.3.3
  url_launcher: ^6.3.2
  shared_preferences: ^2.2.2
  http: ^1.2.0
  device_info_plus: ^12.3.0
  package_info_plus: ^9.0.0
  in_app_review: ^2.0.9
  purchases_flutter: ^10.1.1
  provider: ^6.1.5+1
  purchases_ui_flutter: ^10.1.1

flutter:
  uses-material-design: true
